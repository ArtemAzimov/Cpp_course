ДЗ. Написать то же самое, что было в прошлый раз, но с использованием класса MyList вместо структуры t_list. 

class MyClass
{
    MyClass* next;
    int      data;
// ....
};

Нужно реализовать конструкторы класса MyList который может принимать 2 аргумента, 1 аргумент, и 0 аргументов. То есть: MyList(int, MyList*); 
можете использовать перегрузку и создать 3 отдельных конструктора, а можете задать аргументы по умолчанию. Как посчитаете оптимальным.
И никаких инициализаций типа: int data = 0;
Также добавьте деструктор, который ничего не делает. Т.е. с телом {}

Добавить методы к классу: 
MyClass* createNode(int data);
void pushNode(MyClass* new_node);
void showList();
int getData();

Следите за тем, чтобы ваш указатель на первую ноду указывал на первую ноду и всегда был корректным. Если я (пользователь класса) 
смогу сломать класс, это ваш факап
